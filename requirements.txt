# Core dependencies (no DB driver needed for SQLite dev)
Django==5.0.9
djangorestframework==3.15.2
django-htmx==1.21.0
python-dotenv==1.0.1
whitenoise==6.7.0
django-crispy-forms==2.3
crispy-bootstrap5==2024.2

# PostgreSQL driver – ONLY needed in production or if DATABASE_URL points to PostgreSQL.
# On Windows without PostgreSQL installed, comment this line out for local dev with SQLite.
# Install separately when needed: pip install psycopg2-binary
# (requires Python <=3.12 and PostgreSQL client libs)
psycopg2-binary==2.9.9

# gunicorn – Linux/macOS production server (not needed on Windows dev)
# gunicorn==22.0.0
